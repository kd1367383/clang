#include<stdio.h>
main()
{
	int i, j=0, k,d[10];
	for (i = 0,k=0; j != -999; i++,k++) {
		printf("®”i|‚X‚X‚X‚ÅI—¹jH");
		scanf("%d", &j);
		if (j != -999) {
			d[i] = j;
		}
		if (k = d[10]) {
			break;
		}
	}
	for (i = 0; d[i] < k; i++) {
		printf("%d ", d[i]);
	}
}